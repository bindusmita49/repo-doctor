# ─────────────────────────────────────────────
# Repo Doctor — Python dependencies
# ─────────────────────────────────────────────

# Google Agent Development Kit (ADK) — the core framework
# Includes google-generativeai and grpc transports as sub-dependencies
google-adk>=1.0.0

# Load environment variables from .env files (keeps secrets out of source code)
python-dotenv>=1.0.0

# Web UI for the Repo Doctor front-end (Phase 2+)
streamlit>=1.35.0

# HTTP requests — used to call the GitHub REST API
requests>=2.31.0

# Model Context Protocol (MCP) support for ADK integration
mcp>=1.0.0

